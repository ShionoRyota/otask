<meta name="viewport" content="width=device-width,initial-scale=1">
<style>
 table {
  margin: auto;
  margin-bottom: 30px;
  margin-top: 20px;
  color: #FFFFFF;
 }
.goukei {
  text-align: center;
  color: #FFFFFF;
  margin-top: 20px;
}

@media only screen and (max-width: 750px) {
    .textArea {
      width: 240px;
      font-size: 18px;
    }
 }

 @media only screen and (max-width: 750px) {
    button.button2 {
      width: 144px;
      height: 35px;
      font-size: 18px;
      padding: 0px;
    }
 }

</style>

<%= form_for @task, :html => {:multipart => true}, url: list_tasks_path do |f| %>
<% if @task.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <h2>エラーがあります</h2>
      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<ul>

  <div class="field">
    <br><h9><%= f.label :仕事名 %></h9><br />
    <%= f.text_area :taskname, size: "50x2", style: "font-size:20px", placeholder: "シャフト", class: "textArea" %>
  </div>

  <div class="field">
    <h9><%= f.label :画像 %></h9><br />
    <%= f.fields_for :thumbnails do |thumbnail| %>
    <%= thumbnail.file_field :images %>
    <% end %>

  </div>


  <div class="field">
    <h9><%= f.label :注文番号 %></h9><br />
    <%= f.text_field :order_number, size: "50x1", style: "font-size:20px", placeholder:"1234-5678", class: "textArea" %>
  </div>

  <div class="field">
    <h9><%= f.label :個数 %></h9><br />
    <%= f.text_field :number, id: "tasks_number", size: "50x1", style: "font-size:20px", placeholder: "10本", class: "textArea"%>   </div>

  <div class="field">
    <h9><%= f.label :単価 %></h9><br />
    <%= f.text_field :price, id: "tasks_price", size: "50x1", style: "font-size:20px", placeholder: "1000円", class: "textArea" %>  </div>


<html>
<body>

<div class="goukei">合計金額 (税抜)</div>
 <table border = "1">
   <tr><td id = "answer">円</td></tr>
 </table>

<script type = "text/javascript">
<!--
window.onload = function() {
var number = document.getElementById( "tasks_number" ); // DOM要素を用意しておく
var price = document.getElementById( "tasks_price" ); // DOM要素を用意しておく
var answer = document.getElementById( "answer" ); // DOM要素を用意しておく

number.value = ""; // 初期化
price.value = ""; // 初期化
answer.value = ""; // 初期化

price.onkeyup = function() { // キー入力が終わった瞬間に実行される関数の宣言
answer.innerHTML = parseInt( number.value, 10 ) * parseInt( price.value, 10 ); // 実際の計算
};
}
// -->
</script>
</body>
</html>


 <div class="field">
    <h9><%= f.label :材料代 %></h9><br />
    <%= f.text_field :material_cost, size: "50x1", style: "font-size:20px", placeholder: "材料代1000円", class: "textArea" %>
 </div>

 <div class="field">
    <h9><%= f.label :仲介料 %></h9><br />
    <%= f.text_field :brokerage_fee, size: "50x1", style: "font-size:20px", placeholder: "材料代1000円", class: "textArea" %>
 </div>

 <div class="field">
    <h9><%= f.label :加工代 %></h9><br />
    <%= f.text_field :processing_fee, size: "50x1", style: "font-size:20px", placeholder: "材料代1000円", class: "textArea" %>
 </div>

 <div class="field">
    <h9><%= f.label :作業の所要時間 %></h9><br />
    <%= f.text_field :duration, size: "50x1", style: "font-size:20px", placeholder: "2時間", class: "textArea" %>  </div>



<div class="birth_date_field">
  <h9><%= f.label :納期 %></h9><br>
  <%= raw sprintf(
              f.date_select(
                  :term,
                  use_month_numbers: true,
                  start_year:        2018,
                  end_year:          (Time.now.year + 5),
                  date_separator:    '%s'),
              '年 ', '月 ') + '日' %>
</div>


  <div class="field2">
    <h9><%= f.label :備考 %></h9><br />
    <%= f.text_area :remarks, size: "50x2", style: "font-size:20px", class: "textArea" %>
  </div>


  <div class="actions">
    <button class="button2" type="submit" id="post">保存する</button><br><br>
  </div>

</ul>
<% end %>

<script type="text/javascript">
$(function() {
  $('input[type=file]').after('<span></span>');

  // アップロードするファイルを選択
  $('input[type=file]').change(function() {
    var file = $(this).prop('files')[0];

    // 新幅・高さ
    var new_w = 100;
    var new_h = 100;

    // 画像表示
    var reader = new FileReader();
    reader.onload = function() {
      var img_src = $('<img>').attr('src', reader.result);

      var org_img = new Image();
      org_img.src = reader.result;
      org_img.onload = function() {
        // 元幅・高さ
        var org_w = this.width;
        var org_h = this.height;
        // 幅 ＜ 規定幅 && 高さ ＜ 規定高さ
        if (org_w < new_w && org_h < new_h) {
          // 幅・高さは変更しない
          new_w = org_w;
          new_h = org_h;
        } else {
          // 幅 ＞ 規定幅 || 高さ ＞ 規定高さ
          if (org_w > org_h) {
            // 幅 ＞ 高さ
            var percent_w = new_w / org_w;
            // 幅を規定幅、高さを計算
            new_h = Math.ceil(org_h * percent_w);
          } else if (org_w < org_h) {
            // 幅 ＜高さ
            var percent_h = new_h / org_h;
            // 高さを規定幅、幅を計算
            new_w = Math.ceil(org_w * percent_h);
          }
        }

        // リサイズ画像
        $('span').html($('<canvas>').attr({
          'id': 'canvas',
          'width': new_w,
          'height': new_h
        }));
        var ctx = $('#canvas')[0].getContext('2d');
        var resize_img = new Image(); 
        resize_img.src = reader.result;
        ctx.drawImage(resize_img, 0, 0, new_w, new_h);
      };
    }
    reader.readAsDataURL(file);
  });
});

</script>


<style>
#preview img{
  width: 100px;
}
</style>